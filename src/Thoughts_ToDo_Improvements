* Should use Set for colours rather than List because Set does not allow duplicates, but then need to reimplement CodeGenerator
* Display uses System.out.println to communicate with user, but may need to use different mode of communication
* Adapt tests to different length of code
* Add more tests
* Refactor to see if functional programming can be incorporated
* Test with different length of code and tries


